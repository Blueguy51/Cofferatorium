import SwiftUI
import UIKit
import WebKit

struct MainView: View {
    var body: some View {
        Text("Text Copy")
    }
}

struct MainView_Previews: PreviewProvider {
    static var previews: some View {
        MainView()
    }
}
